spring:
  application:
    name: registry
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD:111111}
      username: user
      profile: ${SPRING_PROFILES_ACTIVE:dev}